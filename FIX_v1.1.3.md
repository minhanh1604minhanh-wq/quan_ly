# v1.1.3 — Root route fix

- Added `GET /` in `server.js` to redirect to `/index.html`.
- Keeps Vercel Express zero-config (no `vercel.json`).
- No changes to Supabase, RBAC, analytics ingestion, AI, or dashboard behavior.
